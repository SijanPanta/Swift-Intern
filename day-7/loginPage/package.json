{
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.2.1"
  },
  "scripts": {
    "dev": "node --watch --env-file=.env server",
    "devStart": "nodemon --env-file=.env server.js"
  }
}
